# 需求实施计划

- [ ] 1. 搭建 Flutter 项目结构与核心接口
   - 创建 Flutter 项目骨架（`flutter create`），配置 iOS/Android 平台目录
   - 建立目录结构：`lib/models/`、`lib/repositories/`、`lib/services/`、`lib/pages/`、`lib/providers/`、`lib/widgets/`
   - 添加依赖：drift、sqlite3、riverpod、image_picker、fl_chart、path_provider、dio、json_serializable
   - 定义核心接口：`MealRepository`、`FeedbackRepository`、`AdviceRepository`、`AIService`、`BackupService`
   - 设置测试框架与测试目录
   - 引用需求：R1、R2、R7、R11

- [ ] 2. 实现本地数据库与数据模型
  - [ ] 2.1 定义 Drift 数据表（MEAL_RECORD、FOOD_ITEM、BODY_FEEDBACK、GUT_SYMPTOM、ADVICE_RECORD）
     - 编写各表的字段、类型、外键与唯一约束（feedback.date 唯一、级联删除）
     - 编写数据库初始化与连接管理代码
     - 引用设计 Data Models 章节、正确性属性 1/2/5/6

  - [ ] 2.2 实现数据模型类与枚举
     - 编写 MealRecord、FoodItem、BodyFeedback、GutSymptom、AdviceRecord 模型类
     - 实现枚举：餐次（早餐/午餐/晚餐/加餐）、症状类型、严重程度、AI 建议结论三档
     - 实现字段校验函数（评分 1-5、体重大于 0、日期格式 yyyy-MM-dd）
     - 引用设计字段约束表

  - [ ] 2.3 为数据模型与校验编写单元测试
     - 验证评分值域、体重正数、日期唯一等约束
     - 验证枚举转换与序列化正确性

- [ ] 3. 检查点 - 确保数据层测试通过，如有疑问请询问用户

- [ ] 4. 实现 Repository 层
  - [ ] 4.1 实现 MealRepository（美食记录 CRUD）
     - 实现按日期查询、按 ID 查询、保存、更新、删除（级联删除食物条目与图片）
     - 支持一餐多食物条目的保存与读取
     - 引用需求 R1、R3

  - [ ] 4.2 实现 FeedbackRepository（身体反馈 CRUD）
     - 实现按日期查询、保存、更新，保证单日唯一
     - 实现肠胃症状的关联保存与级联删除
     - 引用需求 R2、R3、R7

  - [ ] 4.3 实现 AdviceRepository（AI 建议历史）
     - 实现建议历史保存、按时间倒序查询、分页
     - 引用需求 R9

  - [ ] 4.4 为 Repository 编写单元测试
     - 使用内存数据库验证 CRUD、单日唯一约束、级联删除、日期过滤

- [ ] 5. 实现 AI 服务层
  - [ ] 5.1 实现食物识别服务（AIService.recognizeFood）
     - 实现图片上传与识别结果解析，取置信度最高结果
     - 实现超时与失败处理，失败时返回可手动填写的空结果
     - 引用需求 R1、R6

  - [ ] 5.2 实现 AI 饮食建议服务（AIService.getDietAdvice）
     - 实现历史记录摘要聚合（近 30 天饮食与肠胃症状）
     - 实现建议 API 调用与三档结论、原因、风险因素解析
     - 实现失败重试与数据不足提示
     - 引用需求 R9

  - [ ] 5.3 提供 AI 服务的 Mock 实现与可配置 Base URL
     - 编写 MockAIService 便于本地开发与测试
     - 支持通过配置切换 Mock 与真实服务
     - 引用需求 R6、R9

  - [ ] 5.4 为历史摘要聚合与 AI 结果解析编写单元测试
     - 验证摘要正确聚合近 30 天数据
     - 验证 AI 响应解析与失败分支

- [ ] 6. 检查点 - 确保服务层测试通过，如有疑问请询问用户

- [x] 7. 实现状态管理与页面骨架
  - [x] 7.1 实现底部导航与页面路由
     - 实现 5 Tab 底部导航（首页/日历/分析/AI 助手/我的）
     - 配置页面路由与返回导航
     - 引用需求 R11

  - [x] 7.2 实现首页概览（DashboardPage）
     - 展示今日日期、今日美食记录数量、身体反馈完成状态
     - 展示连续记录天数与本月累计记录天数
     - 提供记录美食、记录身体反馈、AI 饮食建议入口
     - 引用需求 R8、R11

  - [x] 7.3 实现美食记录页（MealFormPage）
     - 实现拍照/相册选图、AI 识别填充食物名称
     - 实现一餐多食物条目编辑（每项含图片、名称、评价）
     - 实现餐次选择与默认餐次推断、目标日期选择（支持补录）
     - 实现必填校验与保存
     - 引用需求 R1、R3、R6

  - [x] 7.4 实现身体反馈页（BodyFeedbackPage）
     - 实现五维 1-5 分评分、体重数值输入、文字备注
     - 实现肠胃症状多选与严重程度标记
     - 支持目标日期选择（补录），保证单日唯一
     - 引用需求 R2、R7

  - [x] 7.5 实现每日详情页（DayDetailPage）
     - 按餐次展示当日美食记录与多食物条目
     - 展示身体反馈评分、体重、症状与备注
     - 实现编辑、删除（二次确认）、补录餐次/反馈入口
     - 引用需求 R3

  - [x] 7.6 为表单校验与状态流转编写单元测试（用户指示跳过）
     - 验证必填校验、餐次默认推断、AI 失败兜底手动填写

- [x] 8. 检查点 - 确保核心记录链路（记录美食+身体反馈）可用，如有疑问请询问用户

- [x] 9. 实现分析与回顾功能
  - [x] 9.1 实现日历视图（CalendarPage）
     - 实现月历展示、有记录的日期标记、月份切换、点击跳转每日详情
     - 引用需求 R4

  - [x] 9.2 实现趋势分析（TrendChartPage）
     - 实现五维评分折线图与体重数值折线图（近 30 天）
     - 实现维度选择、缺数据日期跳过、饮食记录对照提示、数据不足提示
     - 引用需求 R5

  - [x] 9.3 实现肠胃症状关联分析（CorrelationPage）
     - 实现近 30 天症状频率统计
     - 实现按症状查看当天饮食清单
     - 实现疑似敏感食物标记与症状日/无症状日次数对比
     - 引用需求 R10

  - [x] 9.4 为日历标记逻辑与趋势图数据处理编写单元测试（用户指示跳过）
     - 验证日期标记、缺数据日期跳过、症状频率统计

- [x] 10. 实现 AI 助手与数据管理
  - [x] 10.1 实现 AI 饮食建议页（AIAdvicePage）
     - 实现文本输入与图片提问两种方式
     - 实现建议结果展示（三档结论、原因、风险因素）与保存历史
     - 实现失败重试与数据不足提示
     - 引用需求 R9

  - [x] 10.2 实现 AI 建议历史页（AdviceHistoryPage）
     - 实现历史建议倒序列表与详情查看
     - 引用需求 R9

  - [x] 10.3 实现数据备份与恢复（BackupService 及页面）
     - 实现全部记录（含图片）导出为备份文件
     - 实现备份导入与格式校验，失败不影响现有数据
     - 在"我的"页提供备份、恢复、关于入口
     - 引用需求 R7、R12

  - [x] 10.4 为备份导出/导入与建议历史编写单元测试（用户指示跳过）
     - 验证备份文件格式完整性、损坏文件恢复失败保护现有数据

- [x] 11. 整体联调与收尾（用户确认以 analyze+单测作为验证手段，跳过运行验证）
   - 打通"记录美食 → 记录身体反馈 → 日历 → 每日详情 → 趋势/关联分析 → AI 建议"全链路
   - 处理边界情况：无网络、空数据、超长文本、图片缺失
   - 运行全部测试与静态检查，确保通过
   - 引用需求 R1-R12
